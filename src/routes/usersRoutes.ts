import { Router } from 'express';
// import * as usersControllers from '../controllers/users.controllers';

const routes = Router();

// routes.post('/users', usersControllers.default.createUser);

export default routes;